# Never commit state (holds the DB password, OIDC cookie secret, and JumpCloud
# client secret in plaintext) or vars.
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars
crash.log
