# Maintainers of airbrx/omnigent
# One bare GitHub username per line. Comments start with #.
#
# This roster is read from main by maintainer_approval.py (the Maintainer
# Approval gate) and by merge-ready/load-maintainers.sh, which the Security
# Scan uses to decide whose PRs are trusted. Listing someone therefore both
# lets them satisfy the gate and exempts their PRs from the scan, so it tracks
# write access: everyone here can merge, and nobody who cannot is listed.
btallman
bissellator
aerickson-airbrx
